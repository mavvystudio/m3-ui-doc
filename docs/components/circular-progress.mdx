# CircularProgress

## Example

```jsx

import CircularProgress from '@mavvy/m3-ui/CircularProgress';
import Text from '@mavvy/m3-ui/Text';

export function Example() {
  return (
    <div>
      <Text>small</Text>
      <CircularProgress
        color="primary"
        size="small"
        indeterminate
      />
      <Text>medium</Text>
      <CircularProgress color="primary" indeterminate />
      <Text>large</Text>
      <CircularProgress
        color="primary"
        size="large"
        indeterminate
      />
    </div>
  );
}


```
import CircularProgress from '@mavvy/m3-ui/CircularProgress';
import Text from '@mavvy/m3-ui/Text';
import List from '@site/src/components/List';

export function Example() {
  return (
    <List>
      <Text>small</Text>
      <CircularProgress indeterminate color="primary" size="small" />
      <Text>medium</Text>
      <CircularProgress indeterminate color="primary" />
      <Text>large</Text>
      <CircularProgress indeterminate color="primary" size="large" />
    </List>
  );
}

<Example />

## Props

|prop|type|description|
|----|----|-----------|
|value|number|value of the current progress|
|color|[Color](/docs/theming/colors)|component color|
|size|[Size](#size)|size of the chip component|
|indeterminate|boolean|if true, shows an indeterminate state|
|className|string|className of the component|
